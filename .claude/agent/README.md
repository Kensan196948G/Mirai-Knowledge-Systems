# .claude/agent/ ディレクトリについて

## 📋 概要

このディレクトリ（`.claude/agent/`）には、SubAgent定義の**簡略版**が含まれています。

## 🔍 ディレクトリ構造

```
Mirai-Knowledge-Systems/
├── .claude/agent/          # ← このディレクトリ（簡略版）
│   ├── ci-specialist.md
│   ├── arch-reviewer.md
│   ├── code-implementer.md
│   ├── ops-runbook.md
│   ├── sec-auditor.md
│   ├── spec-planner.md
│   └── test-designer.md
│
└── .opencode/agent/        # ← 正式なSubAgent定義（詳細版）
    ├── ci-specialist.md
    ├── arch-reviewer.md
    ├── code-implementer.md
    ├── ops-runbook.md
    ├── sec-auditor.md
    ├── spec-planner.md
    ├── test-designer.md
    └── AGENTS.md           # ← 運用ガイド
```

## 🎯 使用するディレクトリ

**OpenCodeは`.opencode/agent/`を使用します。**

- ✅ **使用中**: `.opencode/agent/` - OpenCodeの正式なSubAgent定義ディレクトリ
- 📝 **参考用**: `.claude/agent/` - Claude Code用の簡略版定義

## 📊 違い

| 項目 | `.claude/agent/` | `.opencode/agent/` |
|------|------------------|-------------------|
| **詳細度** | 簡略版 | 詳細版 |
| **Permission設定** | 基本的な設定のみ | プロジェクト固有の詳細設定 |
| **運用ガイド** | なし | AGENTS.md あり |
| **OpenCode認識** | ❌ 認識されない | ✅ 認識される |
| **Model指定** | 明示的に指定 | プロジェクトデフォルト使用 |

## 🚀 SubAgent有効化状態

以下の7つのSubAgentが`opencode.json`で有効化されています：

1. 🎯 **spec-planner** - 要件整理・タスク分解専門
2. 🏗️ **arch-reviewer** - アーキテクチャ・設計レビュー専門
3. 💻 **code-implementer** - コード実装専門
4. 🧪 **test-designer** - テスト設計専門
5. 🚀 **ci-specialist** - GitHub Actions CI/CD専門
6. 🔒 **sec-auditor** - セキュリティ監査専門
7. 📚 **ops-runbook** - 運用手順書作成専門

## 📖 使用方法

SubAgentを使用するには、OpenCodeのコマンドラインで以下のように呼び出します：

```bash
# 例: CI/CD専門家を呼び出す
@ci-specialist "GitHub Actionsのワークフローを最適化して"

# 例: セキュリティ監査を依頼
@sec-auditor "新しく追加したエンドポイントのセキュリティをチェックして"

# 例: 複数のSubAgentを並列実行
@spec-planner "この機能のタスク分解をして" && @code-implementer "タスクに基づいて実装して"
```

## 🔗 関連ドキュメント

- **運用ガイド**: `.opencode/agent/AGENTS.md`
- **OpenCode設定**: `opencode.json`
- **プロジェクト全体**: `.claude/CLAUDE.md`

## 📅 更新履歴

| 日付 | 変更内容 |
|------|----------|
| 2026-01-09 | `.claude/agent/`ディレクトリ作成、README追加 |
| 2026-01-09 | `opencode.json`に7つのSubAgentを有効化 |

---

**注意**: SubAgentの定義を変更する場合は、`.opencode/agent/`内のファイルを編集してください。
