# Mirai Knowledge Systems - サブエージェント運用ガイド

## 🎯 概要

本プロジェクトでは、7体の専門化されたサブエージェントを並列実行することで、効率的な開発を実現します。

## 👥 7体のサブエージェント

| # | エージェント名 | ID | 役割 | 優先度 |
|---|---------------|-----|------|--------|
| 1 | Database Architect | `db-architect` | DB設計・最適化 | 高 |
| 2 | Backend Developer | `backend-dev` | Flask API開発 | 高 |
| 3 | Frontend Developer | `frontend-dev` | WebUI開発 | 高 |
| 4 | Security Auditor | `security-auditor` | セキュリティ監査 | **最高** |
| 5 | QA Engineer | `qa-engineer` | 品質保証・テスト | 高 |
| 6 | DevOps Engineer | `devops` | CI/CD・インフラ | 高 |
| 7 | Docs Writer | `docs-writer` | ドキュメント整備 | 中 |

## 🚀 並列実行パターン

### パターン1: 日常開発（3-4体並列）

```bash
# バックエンド開発時に同時実行
task agent:backend-dev    # API実装
task agent:frontend-dev   # UI実装
task agent:qa-engineer    # テスト作成
task agent:security-auditor # セキュリティ確認
```

### パターン2: 本番デプロイ時（6-7体並列）

```bash
# 全エージェント同時実行
task agent:all
```

### パターン3: 緊急対応（2-3体）

```bash
# セキュリティ対応
task agent:security-auditor
task agent:backend-dev
task agent:qa-engineer
```

## 📁 エージェント設定ファイル

各エージェントの設定は以下のファイルに格納されています：

```
.opencode/agent/
├── 01-db-architect.md     # DB設計担当
├── 02-backend-dev.md      # バックエンド担当
├── 03-frontend-dev.md     # フロントエンド担当
├── 04-security-auditor.md # セキュリティ担当
├── 05-qa-engineer.md      # QA担当
├── 06-devops.md           # DevOps担当
└── 07-docs-writer.md      # ドキュメント担当
```

## 🔧 使用可能なMCPツール

### 接続済みMCP（正常動作）

| MCP名 | 用途 | 設定 |
|-------|------|------|
| `brave-search` | Web検索 | 有効 |
| `github` | GitHub操作 | 有効 |
| `memory` | ナレッジグラフ | 有効 |
| `sequential-thinking` | 順次思考 | 有効 |

### 接続済みMCP（設定済み）

| MCP名 | 用途 | 設定 |
|-------|------|------|
| `sqlite` | SQLite DB操作 | 設定済み |
| `context7` | ドキュメント検索 | 設定済み |
| `playwright` | E2Eテスト | 設定済み |

## 📋 開発ワークフロー

### フェーズ1: 設計（db-architect, devops）

1. **DB設計** (`db-architect`)
   - テーブル設計レビュー
   - インデックス最適化
   - マイグレーション計画

2. **インフラ設計** (`devops`)
   - systemd設定レビュー
   - Nginx設定確認
   - CI/CDパイプライン設計

### フェーズ2: 実装（backend-dev, frontend-dev）

1. **バックエンド実装** (`backend-dev`)
   - APIエンドポイント実装
   - ビジネスロジック実装
   - 認証・認可実装

2. **フロントエンド実装** (`frontend-dev`)
   - UIコンポーネント実装
   - API連携実装
   - UX改善

### フェーズ3: 品質保証（qa-engineer, security-auditor）

1. **セキュリティ監査** (`security-auditor`)
   - XSS/SQLインジェクション診断
   - 認証セキュリティ検証
   - CSPcompatible確認

2. **テスト作成** (`qa-engineer`)
   - ユニットテスト実装
   - 統合テスト実装
   - E2Eテスト実装

### フェーズ4: ドキュメント（docs-writer）

1. **ドキュメント整備** (`docs-writer`)
   - API仕様書更新
   - 運用マニュアル更新
   - 変更履歴記録

## 🎯 成果物管理システム

### memory MCPによるナレッジ管理

```
memory_create_entities    # エンティティ作成
memory_create_relations   # 関係性作成
memory_read_graph         # グラフ読み取り
memory_search_nodes       # ノード検索
```

### 推奨使用方法

```python
# 実装前の要件確認
memory_search_nodes("API設計 ナレッジ")

# 実装後の知見記録
memory_create_entities([{
    "name": "認証実装知見",
    "entityType": "knowledge",
    "observations": ["JWT認証実装完了", "セキュリティ監査通過"]
}])

# 設計判断の記録
memory_create_relations([{
    "from": "認証モジュール",
    "relationType": "uses",
    "to": "Flask-JWT-Extended"
}])
```

## ⚡ 高速化テクニック

### 並列実行ガイドライン

1. **依存関係のないタスクは同時実行**
   - DB設計とUI実装は並列可能
   - テスト作成とドキュメント作成は並列可能

2. **順次実行が必要なタスク**
   - API実装 → テスト実装
   - インフラ構築 → デプロイ

3. **ブロックするタスク**
   - セキュリティ監査は全実装後に実行
   - 本番デプロイは全テスト通過後に実行

## 🔄 GitHub Actions連携

### 自動実行ワークフロー

```
push/pull_request
    ↓
[1] Lint & Code Quality (ruff, flake8, bandit)
    ↓
[2] Unit Tests (pytest, coverage)
    ↓
[3] Integration Tests (PostgreSQL)
    ↓
[4] E2E Tests (Playwright)
    ↓
[5] Security Scan (trufflehog, bandit)
    ↓
[6] Deploy to Development (main/develop)
    ↓
[7] Manual Approval (productionのみ)
    ↓
[8] Deploy to Production
```

### ステータスバッジ

READMEに以下のバッジを追加：

```markdown
![CI/CD](https://github.com/your-org/mirai-knowledge-systems/actions/workflows/ci-cd.yml/badge.svg)
![Coverage](https://img.shields.io/badge/coverage-91%25-green)
![Security](https://img.shields.io/badge/security-audit_passed-green)
```

## 📊 成功指標

| 指標 | 目標値 | 測定方法 |
|------|--------|----------|
| テストカバレッジ | 80%以上 | pytest-cov |
| E2Eテスト成功率 | 90%以上 | Playwright |
| セキュリティスコア | A評価 | Bandit |
| デプロイ所要時間 | 5分以内 | GitHub Actions |
| 並列処理効率 | 3倍以上 | 所要時間比較 |

## 🚨 トラブルシューティング

### MCP接続エラー

```bash
# MCP再起動
npx kill-port 3000
# 設定確認
cat opencode.json | jq '.mcp'
```

### GitHub Actions失敗

```bash
# ログ確認
gh run view <run-id> --log
# 再実行
gh run rerun <run-id>
```

### テスト失敗

```bash
# ローカルでテスト実行
cd backend
source venv_linux_new/bin/activate
pytest tests/ -v --tb=short
```

## 📚 参考リンク

- [GitHub Actions ドキュメント](https://docs.github.com/ja/actions)
- [MCPプロトコル仕様](https://modelcontextprotocol.io/)
- [プロジェクト Wiki](../../wiki)

---

**作成日**: 2026-01-08
**最終更新**: 2026-01-08
**バージョン**: 1.0.0
